#include "vector.h"
#include <stdlib.h>
void vector_init(t_vector *v)
{

	v->
	v->vectorList.size = 0;
	v->vectorList.items = malloc(sizeof(void *) * v->vectorList.capacity);
}

int main(void) {
	t_vector *v;
}